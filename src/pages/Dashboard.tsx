import { AlertTriangle,ArrowUpRight,Building2,CircleDollarSign,Clock3,CreditCard } from "lucide-react";
import { Card,EmptyState,ErrorState,Loading,Metric,PageHeader } from "../components/Ui";
import { euro } from "../lib/format";
import { useAdminData } from "../lib/useAdminData";
import type { DashboardData } from "../types";

export function Dashboard(){const {data,loading,error,reload}=useAdminData<{dashboard:DashboardData}>("dashboard");if(loading)return <Loading/>;if(error||!data)return <ErrorState message={error} retry={reload}/>;const d=data.dashboard;return <><PageHeader eyebrow="Pilotage de la plateforme" title="Tableau de bord" description="Les indicateurs ci-dessous proviennent exclusivement des abonnements et encaissements enregistrés."/><div className="metrics-grid"><Metric label="Entreprises actives" value={d.active_companies} hint={`${d.trial_companies} en essai`}/><Metric label="Abonnements actifs" value={d.active_subscriptions} hint={`${d.past_due_subscriptions} en retard`} tone={d.past_due_subscriptions?"warning":"default"}/><Metric label="MRR" value={euro(d.mrr_cents)} hint="Revenu récurrent mensuel"/><Metric label="ARR" value={euro(d.arr_cents)} hint="MRR × 12"/><Metric label="Nouveaux comptes" value={d.new_companies_month} hint="Ce mois"/><Metric label="Utilisateurs" value={d.active_users} hint="Membres d’entreprises"/><Metric label="Encaissé ce mois" value={euro(d.cash_month_cents)} hint="Paiements Piloz réussis"/><Metric label="Encaissé cette année" value={euro(d.cash_year_cents)} hint={`Remboursements du mois ${euro(d.refunds_month_cents)}`}/></div>{!d.stripe_configured&&<Card className="notice-card"><AlertTriangle/><div><h3>Facturation automatique non configurée</h3><p>Stripe n’est pas connecté. Les MRR et ARR sont calculés depuis les contrats actifs ; les encaissements restent à zéro tant qu’aucun paiement Piloz réel n’est enregistré.</p></div></Card>}<div className="dashboard-panels"><Card><header className="card-header"><div><span className="eyebrow">Activité</span><h2>Situation commerciale</h2></div><Building2/></header><div className="summary-list"><div><span><Clock3/>Essais en cours</span><b>{d.trial_companies}</b></div><div><span><CreditCard/>Paiements en retard</span><b>{d.past_due_subscriptions}</b></div><div><span><ArrowUpRight/>Comptes créés ce mois</span><b>{d.new_companies_month}</b></div></div></Card><Card><header className="card-header"><div><span className="eyebrow">Revenus</span><h2>Données disponibles</h2></div><CircleDollarSign/></header>{d.cash_year_cents||d.mrr_cents?<div className="summary-list"><div><span>Récurrent mensuel</span><b>{euro(d.mrr_cents)}</b></div><div><span>Encaissements annuels</span><b>{euro(d.cash_year_cents)}</b></div><div><span>Revenu net du mois</span><b>{euro(d.cash_month_cents-d.refunds_month_cents)}</b></div></div>:<EmptyState title="Aucun revenu encaissé" description="Aucune donnée réelle de facturation Piloz n’est encore enregistrée."/>}</Card></div></>}
